package com.knol.core.impl

/*
 * Knolder is a case class that has filed names matched same as
 * the table name in the nikhil DB.
 * created a method inside the Knolder Class that is taking
 * default values
 */

case class Knolder(id:Int, name:String, email:String, mobile:String)
